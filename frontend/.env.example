# API base URL. Leave EMPTY for same-origin dev (Vite proxies /api to :8000).
# Set to http://127.0.0.1:8000 to call the backend cross-origin — the mode that
# exercises its CORS_ORIGINS whitelist.
VITE_API_URL=
